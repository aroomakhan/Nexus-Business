export const API_BASE_URL = window.location.hostname === "localhost" 
    ? "http://localhost:5000" 
    : "https://nexus-business-iota.vercel.app";
// Replace the URL below with your actual Vercel backend link
// Replace this with your EXACT backend URL from Vercel
// const BACKEND_URL = "https://nexus-business-iota.vercel.app"; 

// export const API_BASE_URL = window.location.hostname === "localhost" 
//     ? "http://localhost:5000" 
//     : BACKEND_URL;